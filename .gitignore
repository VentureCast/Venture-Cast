# Dependencies (anywhere in the repo)
node_modules/

# Environment & secrets
.env
.env.*
!.env.example
*.xcsettings

# macOS
.DS_Store

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker overrides (if used locally)
docker-compose.prod.yml

# Tooling / AI (keep local)
CLAUDE.md

# --- React Native / iOS ---

# CocoaPods — do not commit; run `cd VentureCast_Frontend-main/ios && pod install`
VentureCast_Frontend-main/ios/Pods/

# Legacy mistaken path (some clones may have this); pods live under ios/Pods/
VentureCast_Frontend-main/Pods/

# React Native iOS codegen / build output (regenerated by Xcode / Metro)
VentureCast_Frontend-main/ios/build/

# Xcode user-specific
**/xcuserdata/
*.xcuserstate
**/DerivedData/

# Local React Native iOS config (machine-specific paths)
**/.xcode.env.local

# --- React Native / Android ---

VentureCast_Frontend-main/android/build/
VentureCast_Frontend-main/android/app/build/
VentureCast_Frontend-main/android/.gradle/
VentureCast_Frontend-main/android/local.properties
VentureCast_Frontend-main/android/.idea/
VentureCast_Frontend-main/android/*.iml

# --- Python (repo-root scripts / tooling)
.venv/
venv/
__pycache__/
*.py[cod]
