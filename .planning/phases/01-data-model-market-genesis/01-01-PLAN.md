---
phase: 01-data-model-market-genesis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - VentureCast_Backend-main/models/Market.js
  - VentureCast_Backend-main/models/MarketState.js
  - VentureCast_Backend-main/models/LedgerAccount.js
  - VentureCast_Backend-main/models/LedgerEntry.js
  - VentureCast_Backend-main/models/Order.js
  - VentureCast_Backend-main/models/Trade.js
  - VentureCast_Backend-main/models/PriceCandle.js
  - VentureCast_Backend-main/models/RiskEvent.js
  - VentureCast_Backend-main/models/AdminAction.js
autonomous: true
requirements: [LEDG-04]

must_haves:
  truths:
    - "All nine AMM Mongoose models exist and register without error"
    - "Order.idempotencyKey has a unique index"
    - "MarketState.marketId has a unique index"
    - "Model.init() succeeds for every model (collections + indexes build cleanly)"
  artifacts:
    - path: "VentureCast_Backend-main/models/Market.js"
      provides: "Per-creator market config schema"
      contains: "mongoose.model('Market'"
    - path: "VentureCast_Backend-main/models/MarketState.js"
      provides: "Optimistic-lock hot state with unique marketId"
      contains: "mongoose.model('MarketState'"
    - path: "VentureCast_Backend-main/models/LedgerAccount.js"
      provides: "Balance projection per account key (unique accountKey)"
      contains: "mongoose.model('LedgerAccount'"
    - path: "VentureCast_Backend-main/models/LedgerEntry.js"
      provides: "Immutable double-entry postings (accountKey/delta/unit)"
      contains: "mongoose.model('LedgerEntry'"
    - path: "VentureCast_Backend-main/models/Order.js"
      provides: "Quote-to-order lifecycle with unique idempotencyKey"
      contains: "mongoose.model('Order'"
    - path: "VentureCast_Backend-main/models/Trade.js"
      provides: "Immutable filled trade record"
      contains: "mongoose.model('Trade'"
    - path: "VentureCast_Backend-main/models/PriceCandle.js"
      provides: "OHLC candle schema (no candle-building logic)"
      contains: "mongoose.model('PriceCandle'"
    - path: "VentureCast_Backend-main/models/RiskEvent.js"
      provides: "Risk audit record"
      contains: "mongoose.model('RiskEvent'"
    - path: "VentureCast_Backend-main/models/AdminAction.js"
      provides: "Admin operation audit record"
      contains: "mongoose.model('AdminAction'"
  key_links:
    - from: "models/MarketState.js"
      to: "MongoDB unique index"
      via: "unique: true on marketId field"
      pattern: "marketId.*unique: true"
    - from: "models/Order.js"
      to: "MongoDB unique index"
      via: "unique: true on idempotencyKey field"
      pattern: "idempotencyKey.*unique: true"
---

<objective>
Define the nine AMM Mongoose models that every later phase reads from or writes to.

Purpose: Establish the exact field names, units (integer cents / integer units), and indexes
the whole AMM milestone depends on. Getting field names or units wrong here propagates into
every later service and test, so these schemas are copied verbatim from the research.
Output: Nine new model files in `VentureCast_Backend-main/models/` — additive only, no
existing file is touched. No runtime logic; just schema definitions with correct indexes.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-model-market-genesis/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

# Schema style to match (verbose per-field comments, inline index, schema.index() post-block, pre('save') updatedAt):
@VentureCast_Backend-main/models/Transaction.js

<interfaces>
<!-- The RESEARCH.md "Complete Schema Designs (All 9 Collections)" section (lines 335-530) -->
<!-- contains the EXACT field names, types, units, enums, and index strategy for every model. -->
<!-- Copy those designs verbatim. Key invariants the executor must honor: -->

CONVENTIONS (non-negotiable):
- CommonJS only: `const mongoose = require('mongoose')` ... `module.exports = mongoose.model('Name', schema)`. NO import/export.
- All money fields are integer cents (Number). All supply/qty fields are integer units (Number). NEVER floats/dollars.
- Use `unique: true` INLINE on the field for: MarketState.marketId, Order.idempotencyKey, LedgerAccount.accountKey.
- Use `schema.index({...})` AFTER the schema block for compound/supporting indexes.
- Add `pre('save')` updatedAt hook ONLY on models that carry `updatedAt`: Market, MarketState, Order.
- Models WITHOUT updatedAt (immutable audit/ledger records): LedgerAccount, LedgerEntry, Trade, PriceCandle, RiskEvent, AdminAction. Do NOT add a save hook to these.
- Schema variable naming: PascalCase + `Schema` suffix (e.g. `MarketStateSchema`).

ACCOUNTKEY CONVENTION (document in LedgerAccount + LedgerEntry comments — later phases depend on it):
  user_cash:<userId>            -> cents
  user_pos:<userId>:<marketId>  -> shares
  market_reserve:<marketId>     -> cents
  platform_fees                 -> cents
  platform_funding              -> cents   (debited at genesis; may run negative — document as bookkeeping artifact)

DELTA SIGN CONVENTION (document in LedgerEntry comment):
  positive = credit (money/shares entering account); negative = debit (leaving).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the four core ledger + market models</name>
  <files>VentureCast_Backend-main/models/Market.js, VentureCast_Backend-main/models/MarketState.js, VentureCast_Backend-main/models/LedgerAccount.js, VentureCast_Backend-main/models/LedgerEntry.js</files>
  <action>
Create the four foundational models using the exact schema designs in 01-RESEARCH.md
("Complete Schema Designs" #1 Market, #2 MarketState, #3 LedgerAccount, #4 LedgerEntry).

For each file follow Transaction.js style: `const mongoose = require('mongoose')` at top,
`PascalCaseSchema` variable, per-field comments stating the unit, `module.exports = mongoose.model(...)`.

Specifics:
- Market.js: streamerId (ObjectId ref Streamer, index), P0_cents, k_num, k_den, tier enum ['1','2','3'],
  status enum ['active','paused'] default 'active', spreadBps, feeBps, createdAt, updatedAt. Add
  `schema.index({ streamerId: 1 })` and a pre('save') updatedAt hook.
- MarketState.js: marketId (ObjectId ref Market, required, `unique: true` INLINE), supply default 0,
  reserveCents, reserveFloorCents, lastPriceCents, version default 0, updatedAt. pre('save') updatedAt hook.
  Add a schema comment noting Phase 4 will mutate version via updateOne({_id,version},{$inc:{version:1}})
  which bypasses pre('save') (updatedAt is informational only).
- LedgerAccount.js: accountKey (String, required, `unique: true`, index), balance default 0,
  unit enum ['cents','shares'] required, updatedAt. NO pre('save') hook (updated via $inc findOneAndUpdate).
  Document the five accountKey templates AND the platform_funding-may-go-negative bookkeeping note in comments.
- LedgerEntry.js: tradeId (ObjectId ref Trade, default null, index), accountKey (String, required, index),
  delta (Number, required), unit enum ['cents','shares'] required, note default null, createdAt index.
  NO updatedAt (immutable). Document the delta sign convention (positive=credit, negative=debit).
  Add `schema.index({ tradeId: 1 })` and `schema.index({ accountKey: 1, createdAt: -1 })`.
  </action>
  <verify>
    <automated>cd VentureCast_Backend-main && node -e "['Market','MarketState','LedgerAccount','LedgerEntry'].forEach(m=>{require('./models/'+m);}); console.log('OK')"</automated>
  </verify>
  <done>All four files load without error; MarketState.marketId and LedgerAccount.accountKey have unique:true inline; LedgerEntry/LedgerAccount have no pre('save') hook.</done>
</task>

<task type="auto">
  <name>Task 2: Create the five lifecycle + audit models</name>
  <files>VentureCast_Backend-main/models/Order.js, VentureCast_Backend-main/models/Trade.js, VentureCast_Backend-main/models/PriceCandle.js, VentureCast_Backend-main/models/RiskEvent.js, VentureCast_Backend-main/models/AdminAction.js</files>
  <action>
Create the five remaining models using the exact schema designs in 01-RESEARCH.md
("Complete Schema Designs" #5 Order, #6 Trade, #7 PriceCandle, #8 RiskEvent, #9 AdminAction).

Specifics:
- Order.js: userId, marketId, side enum ['buy','sell'], qty (integer units), idempotencyKey
  (String, required, `unique: true` INLINE — this is the EXEC-03 idempotency gate), status enum
  ['pending','filled','rejected','expired'] default 'pending', minReceived default null,
  maxCost default null, quoteId default null, expiresAt required, createdAt, updatedAt.
  pre('save') updatedAt hook. Add `schema.index({ userId: 1, marketId: 1, createdAt: -1 })`
  and `schema.index({ status: 1, expiresAt: 1 })`.
- Trade.js: orderId, marketId, userId, side, qty, grossCents, feeCents, spreadCents, netCents,
  avgPriceCents, supplyBefore, supplyAfter, createdAt index. All money integer cents, supply
  integer units. NO updatedAt (immutable). Add `schema.index({ marketId: 1, createdAt: -1 })`
  and `schema.index({ userId: 1, createdAt: -1 })`.
- PriceCandle.js: marketId, interval enum ['1m','5m','1h','1d'], open/high/low/close (integer cents),
  volume default 0 (integer units), ts (Date, required, index). NO candle-building logic — schema only.
  Add a UNIQUE compound index `schema.index({ marketId: 1, interval: 1, ts: 1 }, { unique: true })`.
- RiskEvent.js: marketId, userId default null, type (String, required), detail
  (mongoose.Schema.Types.Mixed default {}), createdAt index. NO updatedAt. Add the three
  schema.index() compounds from the research (marketId+createdAt, userId+createdAt, type+createdAt).
- AdminAction.js: adminId, action (String, required), target (String, required), before
  (Mixed default null), after (Mixed default null), createdAt index. NO updatedAt. Add
  `schema.index({ adminId: 1, createdAt: -1 })` and `schema.index({ action: 1, createdAt: -1 })`.
  </action>
  <verify>
    <automated>cd VentureCast_Backend-main && node -e "['Order','Trade','PriceCandle','RiskEvent','AdminAction'].forEach(m=>{require('./models/'+m);}); console.log('OK')"</automated>
  </verify>
  <done>All five files load without error; Order.idempotencyKey has unique:true inline; PriceCandle has a unique compound index on marketId+interval+ts; Trade/PriceCandle/RiskEvent/AdminAction have no pre('save') hook.</done>
</task>

</tasks>

<verification>
All nine models load and register, and indexes build cleanly under the in-memory replset:

```bash
cd VentureCast_Backend-main && node -e "
const m=['Market','MarketState','LedgerAccount','LedgerEntry','Order','Trade','PriceCandle','RiskEvent','AdminAction'];
m.forEach(x=>require('./models/'+x));
console.log('all 9 models loaded:', m.length);
"
```

The `Model.init()` success across all nine is proven by the genesis test in Plan 02 (Wave 0
test file calls `.init()` on every model in beforeAll). Plan 01 only needs the files to load
and register without error.
</verification>

<success_criteria>
- The nine model files exist in `VentureCast_Backend-main/models/`.
- `MarketState.marketId` and `Order.idempotencyKey` carry `unique: true` (LEDG-04a / EXEC-03 prerequisite).
- All money fields are integer cents, all supply/qty fields are integer units (no floats/dollars).
- Immutable models (LedgerEntry, LedgerAccount, Trade, PriceCandle, RiskEvent, AdminAction) have no pre('save') updatedAt hook.
- All files use CommonJS (require/module.exports).
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-model-market-genesis/01-01-SUMMARY.md`
documenting the nine models created, their unique indexes, and the accountKey/delta conventions
embedded in the ledger model comments (Plan 02's genesis service and all later phases rely on them).
</output>
