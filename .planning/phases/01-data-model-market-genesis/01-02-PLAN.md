---
phase: 01-data-model-market-genesis
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - VentureCast_Backend-main/tests/unit/genesis.test.js
  - VentureCast_Backend-main/tests/helpers/ammFixtures.js
  - VentureCast_Backend-main/services/amm/genesisService.js
autonomous: true
requirements: [LEDG-04]

must_haves:
  truths:
    - "Model.init() succeeds for all nine txn-involved models (LEDG-04a)"
    - "openMarket() creates a Market + MarketState at supply s0=0, price=P0, version=0 (LEDG-04b)"
    - "Genesis posts two balanced LedgerEntries (platform_funding -floor, market_reserve +floor) that sum to exactly zero (LEDG-04c)"
    - "Genesis is atomic — an aborted transaction leaves zero Market, MarketState, LedgerEntry, and LedgerAccount docs (LEDG-04d)"
    - "LedgerAccount projections after genesis equal the summed entry deltas (platform_funding -floor, market_reserve +floor)"
  artifacts:
    - path: "VentureCast_Backend-main/tests/unit/genesis.test.js"
      provides: "Atomicity gate covering LEDG-04a..d"
      min_lines: 60
      contains: "atomic rollback"
    - path: "VentureCast_Backend-main/tests/helpers/ammFixtures.js"
      provides: "createTestMarket(params) factory reused by later AMM phases"
      exports: ["createTestMarket"]
    - path: "VentureCast_Backend-main/services/amm/genesisService.js"
      provides: "openMarket() atomic genesis + GenesisError class"
      exports: ["openMarket", "GenesisError"]
  key_links:
    - from: "services/amm/genesisService.js"
      to: "models Market/MarketState/LedgerEntry/LedgerAccount"
      via: "single mongoose.startSession() transaction"
      pattern: "startTransaction|commitTransaction"
    - from: "services/amm/genesisService.js"
      to: "double-entry invariant"
      via: "debit platform_funding + credit market_reserve sum to zero"
      pattern: "debitEntry.delta \\+ creditEntry.delta"
    - from: "tests/unit/genesis.test.js"
      to: "openMarket()"
      via: "require after connectTestDB + Model.init() on all 9 models"
      pattern: "openMarket"
---

<objective>
Write the Wave 0 atomicity test (RED first), the reusable AMM market fixture, and the
`genesisService.openMarket()` function that makes the test GREEN — opening a market atomically
and seeding its reserve to the floor via a balanced double-entry posting.

Purpose: This is the LEDG-04 gate. `genesis.test.js` is the success criterion for the whole
phase and the template every later ledger test follows. The genesis posting establishes the
sum-to-zero double-entry invariant that every future trade posting must also satisfy.
Output: `services/amm/genesisService.js` (openMarket + GenesisError), `tests/unit/genesis.test.js`
(LEDG-04a..d), and `tests/helpers/ammFixtures.js` (createTestMarket factory for later phases).
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-model-market-genesis/01-RESEARCH.md
@.planning/phases/01-data-model-market-genesis/01-VALIDATION.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md

# The genesis service must follow this exact transaction pattern (new Doc().save({session}), abort/commit, finally endSession):
@VentureCast_Backend-main/services/tradeService.js

<interfaces>
<!-- The genesis service signature, body, and the genesis.test.js skeleton are given VERBATIM in -->
<!-- 01-RESEARCH.md "Pattern 3: Atomic Genesis Service Function" (lines 175-302) and "Key Test -->
<!-- Structure for genesis.test.js" (lines 723-754). Use those exactly. Key contracts: -->

GENESIS SERVICE (services/amm/genesisService.js):
  class GenesisError extends Error { constructor(message, statusCode, details={}) {...} }  // mirrors TradeError
  async function openMarket(params)  // params: { streamerId, P0_cents=100, k_num=1, k_den=10,
                                     //           tier='1', spreadBps=50, feeBps=100, reserveFloorCents }
  // Returns { market, marketState }. Throws GenesisError(400) if reserveFloorCents missing/<=0
  // or not an integer. ONE session: create Market -> MarketState(supply 0, reserveCents=floor,
  // reserveFloorCents, lastPriceCents=P0_cents, version 0) -> two LedgerEntry saves
  // (platform_funding delta -floor, market_reserve:<id> delta +floor, unit 'cents') ->
  // two LedgerAccount $inc upserts -> assert debit.delta + credit.delta === 0 -> commit.
  module.exports = { GenesisError, openMarket };

TEST HELPERS available (tests/helpers/db.js): connectTestDB(), clearTestDB(), disconnectTestDB().

TEST GOTCHAS (TESTING.md — mandatory):
- Import openMarket and the 9 models AFTER connectTestDB() inside beforeAll.
- Call `await Model.init()` on ALL nine models in beforeAll (Market, MarketState, LedgerAccount,
  LedgerEntry, Order, Trade, PriceCandle, RiskEvent, AdminAction) — transactions fail otherwise.
- afterEach -> clearTestDB(); afterAll -> disconnectTestDB().
- The four test blocks MUST contain the literal -t name substrings: "Model.init", "genesis creates",
  "ledger sum", "atomic rollback" (the VALIDATION.md per-task commands grep these).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write genesis.test.js (RED) + ammFixtures.js</name>
  <files>VentureCast_Backend-main/tests/unit/genesis.test.js, VentureCast_Backend-main/tests/helpers/ammFixtures.js</files>
  <behavior>
    - "Model.init": all nine models import and `await Model.init()` resolves (LEDG-04a).
    - "genesis creates": openMarket({streamerId, P0_cents:100, reserveFloorCents:10000}) returns a
      Market and a MarketState with supply===0, lastPriceCents===100, version===0,
      reserveCents===10000, reserveFloorCents===10000 (LEDG-04b).
    - "ledger sum": after genesis, exactly two LedgerEntry docs exist; their deltas are -10000
      (accountKey 'platform_funding') and +10000 (accountKey 'market_reserve:<marketId>'); the
      sum of all LedgerEntry.delta === 0; LedgerAccount projections match (-10000 / +10000) (LEDG-04c).
    - "atomic rollback": calling openMarket with an invalid param (e.g. reserveFloorCents omitted
      or <= 0, or by forcing a mid-transaction throw) leaves ZERO Market, MarketState, LedgerEntry,
      and LedgerAccount docs — confirming the whole transaction rolled back together (LEDG-04d).
  </behavior>
  <action>
Write `tests/unit/genesis.test.js` using the skeleton in 01-RESEARCH.md lines 723-754:
connectTestDB in beforeAll, require openMarket + all 9 models after connect, call `await Model.init()`
on all nine (via Promise.all), clearTestDB in afterEach, disconnectTestDB in afterAll.

Create four describe/it blocks whose names contain the literal substrings "Model.init",
"genesis creates", "ledger sum", "atomic rollback" so the VALIDATION.md `-t` commands match.
Assert exact integer values (10000 cents floor, supply 0, version 0, P0 100). For "atomic rollback",
trigger an abort by passing an invalid param OR by stubbing one of the saves to throw, then query
each collection and assert countDocuments === 0.

Also create `tests/helpers/ammFixtures.js` exporting `createTestMarket(overrides = {})` — a thin
factory that calls `openMarket({ streamerId: new mongoose.Types.ObjectId().toString(), P0_cents: 100,
k_num: 1, k_den: 10, tier: '1', spreadBps: 50, feeBps: 100, reserveFloorCents: 10000, ...overrides })`
and returns its { market, marketState }. Use CommonJS. Match the style of tests/helpers/fixtures.js.
This factory is reused by Phases 2-6, so keep its signature stable.

RED step: run the test now — it MUST fail (genesisService.js does not exist yet). Commit the
failing test: `test(01-02): add failing genesis atomicity test for LEDG-04`.
  </action>
  <verify>
    <automated>cd VentureCast_Backend-main && npx jest tests/unit/genesis.test.js --runInBand --forceExit 2>&1 | grep -Eiq "Cannot find module|genesisService|fail|✕|FAIL" && echo "RED-CONFIRMED"</automated>
  </verify>
  <done>genesis.test.js exists with four named blocks (Model.init / genesis creates / ledger sum / atomic rollback); ammFixtures.js exports createTestMarket; the suite is RED because genesisService.js is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement genesisService.openMarket() to GREEN</name>
  <files>VentureCast_Backend-main/services/amm/genesisService.js</files>
  <behavior>
    - openMarket() commits Market + MarketState + 2 LedgerEntries + 2 LedgerAccount upserts in one txn.
    - reserveFloorCents missing, <= 0, or non-integer -> throws GenesisError(400) before any write.
    - The two genesis entries sum to exactly zero; assertion throws GenesisError(500) otherwise.
    - All four "Model.init / genesis creates / ledger sum / atomic rollback" blocks pass.
  </behavior>
  <action>
Create `services/amm/genesisService.js` implementing the function in 01-RESEARCH.md lines 175-302
VERBATIM (it is already correct and convention-compliant). Key points to preserve:
- `class GenesisError extends Error` with statusCode + details (mirrors TradeError).
- Guard: `if (!reserveFloorCents || reserveFloorCents <= 0 || !Number.isInteger(reserveFloorCents))`
  throw GenesisError('reserveFloorCents must be a positive integer', 400, {}). (Add the
  Number.isInteger check per RESEARCH Pitfall 4 even though the snippet only shows <=0.)
- Single `mongoose.startSession()` + `session.startTransaction()`.
- Create Market via `new Market({...}).save({ session })`. Create MarketState with marketId=market._id,
  supply 0, reserveCents=reserveFloorCents, reserveFloorCents, lastPriceCents=P0_cents, version 0,
  via `new MarketState({...}).save({ session })`.
- reserveKey = `market_reserve:${market._id}`; fundingKey = 'platform_funding'.
- Two LedgerEntry saves: debit fundingKey delta -reserveFloorCents, credit reserveKey delta
  +reserveFloorCents, unit 'cents', tradeId null, with genesis notes. Use new+save({session}).
- Two LedgerAccount.findOneAndUpdate $inc upserts threading { upsert: true, session } in the options
  object (RESEARCH Pitfall 5). Funding account unit 'cents' via $setOnInsert.
- Invariant: `if (debitEntry.delta + creditEntry.delta !== 0) throw GenesisError(...500...)`.
- commit; on error abortTransaction + rethrow; finally endSession. Log via utils/logger.
- `module.exports = { GenesisError, openMarket }`. CommonJS only.

GREEN step: run the test — all four blocks must pass. Commit: `feat(01-02): implement atomic market genesis (LEDG-04)`.
  </action>
  <verify>
    <automated>cd VentureCast_Backend-main && npx jest tests/unit/genesis.test.js --runInBand --forceExit</automated>
  </verify>
  <done>All four genesis.test.js blocks pass; openMarket creates a market at s0=0/P0/version=0, seeds reserve to floor with two balanced entries summing to zero, and rolls back fully on abort.</done>
</task>

</tasks>

<verification>
Quick gate (per VALIDATION.md sampling — after every task):

```bash
cd VentureCast_Backend-main && npx jest tests/unit/genesis.test.js --runInBand --forceExit
```

Per-requirement assertions (LEDG-04a..d) must each pass:

```bash
cd VentureCast_Backend-main && npx jest tests/unit/genesis.test.js -t "Model.init"     --runInBand --forceExit
cd VentureCast_Backend-main && npx jest tests/unit/genesis.test.js -t "genesis creates" --runInBand --forceExit
cd VentureCast_Backend-main && npx jest tests/unit/genesis.test.js -t "ledger sum"      --runInBand --forceExit
cd VentureCast_Backend-main && npx jest tests/unit/genesis.test.js -t "atomic rollback" --runInBand --forceExit
```

Full-suite regression gate (per wave merge — confirms no break to existing trade/auth/stripe tests):

```bash
cd VentureCast_Backend-main && npm test
```
</verification>

<success_criteria>
- `genesis.test.js` passes all four blocks (LEDG-04a Model.init, LEDG-04b genesis creates,
  LEDG-04c ledger sum, LEDG-04d atomic rollback).
- `openMarket()` creates Market + MarketState (supply 0, price P0, version 0) and posts a balanced
  platform_funding -> market_reserve pair summing to exactly zero, all in one transaction.
- An aborted genesis leaves zero docs across all four collections (atomicity proven on MongoMemoryReplSet).
- `ammFixtures.createTestMarket` is exported for reuse by later phases.
- Full backend suite (`npm test`) stays green — no regression.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-model-market-genesis/01-02-SUMMARY.md`
documenting: the openMarket(params) signature and return shape, the GenesisError class, the exact
genesis double-entry posting (platform_funding -floor / market_reserve:<id> +floor), the
createTestMarket fixture signature, and a note that platform_funding intentionally runs negative
as a documented bookkeeping artifact. Phases 2-6 consume openMarket and createTestMarket.
</output>
