---
phase: 05-api-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - VentureCast_Backend-main/models/User.js
  - VentureCast_Backend-main/middleware/requireAdmin.js
  - VentureCast_Backend-main/middleware/schemas/ammSchemas.js
  - VentureCast_Backend-main/controllers/ammController.js
  - VentureCast_Backend-main/routes/amm.js
  - VentureCast_Backend-main/index.js
  - VentureCast_Backend-main/tests/helpers/fixtures.js
  - VentureCast_Backend-main/tests/integration/amm.test.js
autonomous: true
requirements: [API-01]
must_haves:
  truths:
    - "GET /markets returns each tradable market's id, streamerId, tier, status, supply, priceCents, reserveCents"
    - "GET /markets/:id returns one market's config plus its current MarketState"
    - "GET /markets works with no JWT (optionalAuth) and does not 401"
    - "A user with isAdmin=true passes requireAdmin; a non-admin gets 403; no JWT gets 401"
    - "The amm router is mounted in index.js after the body parsers, behind a rate limiter, before the 404 handler"
  artifacts:
    - path: "VentureCast_Backend-main/routes/amm.js"
      provides: "Express router for /markets read endpoints (mounted at /)"
      contains: "router.get('/markets'"
    - path: "VentureCast_Backend-main/controllers/ammController.js"
      provides: "listMarkets + getMarket controller handlers delegating to services"
      exports: ["listMarkets", "getMarket"]
    - path: "VentureCast_Backend-main/middleware/requireAdmin.js"
      provides: "Admin guard middleware (req.user.isAdmin -> next, else 403)"
      min_lines: 8
    - path: "VentureCast_Backend-main/middleware/schemas/ammSchemas.js"
      provides: "Joi schemas for amm endpoints (marketIdParam to start)"
      contains: "marketIdParam"
    - path: "VentureCast_Backend-main/models/User.js"
      provides: "isAdmin boolean field defaulting to false"
      contains: "isAdmin"
    - path: "VentureCast_Backend-main/tests/integration/amm.test.js"
      provides: "Wave 0 supertest scaffold + GET /markets tests"
      contains: "GET /markets"
  key_links:
    - from: "VentureCast_Backend-main/index.js"
      to: "VentureCast_Backend-main/routes/amm.js"
      via: "app.use mount with a rate limiter"
      pattern: "require\\('\\./routes/amm'\\)"
    - from: "VentureCast_Backend-main/controllers/ammController.js"
      to: "VentureCast_Backend-main/services/amm/pricing/curve.js"
      via: "priceCents(supply, curveParams) to compute current price"
      pattern: "priceCents"
---

<objective>
Stand up the Phase 5 AMM API foundation: the new route/controller/schema/middleware files
(mirroring the existing trade.js/tradeController.js structure exactly), the `User.isAdmin`
field, the `requireAdmin` guard, the admin JWT test helper, the Wave 0 integration-test
scaffold, and the read-only market endpoints (API-01). This plan OWNS the three cross-file
edits — `index.js` (route mount), `models/User.js` (isAdmin), `tests/helpers/fixtures.js`
(admin token) — so the quote/order plan (05-02) and admin plan (05-03) can extend the
already-mounted router and reuse the admin helper without touching those files again.

Purpose: every later API plan depends on the router being mounted, isAdmin existing, and the
admin token helper existing. Doing it once here removes the cross-file sequencing hazard.
Output: routes/amm.js, controllers/ammController.js, middleware/requireAdmin.js,
middleware/schemas/ammSchemas.js, User.isAdmin, index.js mount, fixtures admin helper,
tests/integration/amm.test.js with GET /markets coverage.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-api-surface/05-VALIDATION.md

# Patterns to MIRROR exactly (CommonJS, Joi via validate(), rate limiters, authenticateToken)
@VentureCast_Backend-main/routes/trade.js
@VentureCast_Backend-main/controllers/tradeController.js
@VentureCast_Backend-main/middleware/validate.js
@VentureCast_Backend-main/middleware/schemas/tradeSchemas.js
@VentureCast_Backend-main/middleware/auth.js
@VentureCast_Backend-main/middleware/rateLimiters.js
@VentureCast_Backend-main/index.js
@VentureCast_Backend-main/models/User.js
@VentureCast_Backend-main/tests/helpers/fixtures.js
@VentureCast_Backend-main/tests/integration/trade.test.js

<interfaces>
<!-- Extracted from codebase — use these directly, no exploration needed. -->

models/Market.js fields: { _id, streamerId, P0_cents, k_num, k_den, tier ('1'|'2'|'3'),
  status ('active'|'paused'), spreadBps, feeBps, createdAt, updatedAt }

models/MarketState.js fields: { _id, marketId (unique), supply, reserveCents,
  reserveFloorCents, lastPriceCents, version, updatedAt }

services/amm/pricing/curve.js exports:
  priceCents(s, { P0_cents, k_num, k_den }) -> integer cents   // current price at supply s
  buyCostCents(s0, delta, params), sellPayoutCents(s0, delta, params)

middleware/validate.js: validate(schema, source='body') -> express middleware; 400 on fail.
middleware/auth.js: authenticateToken (401 no token / 403 bad token, sets req.user + req.userId),
  optionalAuth (never fails), verifyOwnership.

middleware/rateLimiters.js exports: { authLimiter, paymentLimiter, tradeLimiter, apiLimiter }.

tests/helpers/fixtures.js exports: createTestUser(overrides), createTestStreamer,
  generateAuthToken(userId) -> JWT signed with JWT_SECRET (default 'test-jwt-secret-key').
tests/helpers/ammFixtures.js exports: createTestMarket(overrides) ->
  { market, marketState } via genesisService.openMarket (defaults: P0=100, k=1/10, tier '1',
  spread 50bps, fee 100bps, reserveFloorCents 10000).
tests/helpers/db.js exports: connectTestDB, clearTestDB, disconnectTestDB (MONGODB_URI is a
  MongoMemoryReplSet from tests/setup.js — transactions work).

index.js mount order (CRITICAL): webhook(raw) -> helmet -> cors -> requestLogger ->
  express.json -> session/passport -> health GET / -> API routes WITH rate limiters ->
  404 handler -> errorHandler. New routes go in the "API Routes with rate limiters" block,
  BEFORE the 404 handler.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0 (Wave 0): Integration-test scaffold + admin JWT helper + User.isAdmin</name>
  <files>VentureCast_Backend-main/tests/integration/amm.test.js, VentureCast_Backend-main/tests/helpers/fixtures.js, VentureCast_Backend-main/models/User.js</files>
  <action>
    1. Add `isAdmin: { type: Boolean, default: false }` to UserSchema in models/User.js (place it
       near the basic-info block; do not disturb existing fields/virtuals/methods).
    2. In tests/helpers/fixtures.js add `generateAdminToken(userId)` that signs the SAME JWT shape
       as generateAuthToken (`jwt.sign({ id: userId.toString() }, JWT_SECRET, { expiresIn: '1h' })`).
       The token only needs to authenticate; admin authority comes from the User's isAdmin field —
       so also export it. (createTestUser already takes overrides, so admin users are made via
       `createTestUser({ isAdmin: true })`.) Export generateAdminToken alongside the existing exports.
    3. Create tests/integration/amm.test.js as the Wave 0 scaffold, mirroring trade.test.js:
       - require supertest, { connectTestDB, clearTestDB, disconnectTestDB } from ../helpers/db,
         { createTestUser, generateAuthToken, generateAdminToken } from ../helpers/fixtures,
         { createTestMarket } from ../helpers/ammFixtures, and `const app = require('../../index')`.
       - beforeAll connectTestDB(); afterEach clearTestDB(); afterAll disconnectTestDB().
       - Add a `describe('GET /markets')` block with the tests in Task 2's <verify>. (Later plans
         05-02 / 05-03 APPEND their own describe blocks to THIS file.)
    Reference: tests/integration/trade.test.js is the exact supertest pattern to copy.
  </action>
  <verify>
    <automated>cd VentureCast_Backend-main && node -e "require('./models/User'); require('./tests/helpers/fixtures').generateAdminToken; console.log('ok')"</automated>
  </verify>
  <done>User.isAdmin exists (default false); fixtures exports generateAdminToken; amm.test.js exists with a GET /markets describe block and the db/app boot wiring.</done>
</task>

<task type="auto">
  <name>Task 1: requireAdmin middleware + ammSchemas (marketIdParam)</name>
  <files>VentureCast_Backend-main/middleware/requireAdmin.js, VentureCast_Backend-main/middleware/schemas/ammSchemas.js</files>
  <action>
    1. Create middleware/requireAdmin.js: a function `(req, res, next)` that returns
       `401 { error: 'Authentication required' }` if `!req.user`, returns
       `403 { error: 'Admin access required' }` if `!req.user.isAdmin`, else `next()`.
       It is ALWAYS chained AFTER authenticateToken (which sets req.user). Mirror the small
       style of middleware/auth.js verifyOwnership. Export the function directly
       (`module.exports = requireAdmin`).
    2. Create middleware/schemas/ammSchemas.js mirroring tradeSchemas.js: define
       `const objectId = Joi.string().hex().length(24);` and export `marketIdParam =
       Joi.object({ id: objectId.required() })`. This file is the home for ALL amm Joi schemas;
       plans 05-02 and 05-03 ADD quoteSchema/orderSchema/admin schemas to it. Export an object
       with marketIdParam (and keep the export object shape so later plans extend it).
  </action>
  <verify>
    <automated>cd VentureCast_Backend-main && node -e "const r=require('./middleware/requireAdmin'); const {marketIdParam}=require('./middleware/schemas/ammSchemas'); if(typeof r!=='function'||!marketIdParam) throw new Error('missing'); console.log('ok')"</automated>
  </verify>
  <done>requireAdmin exports a middleware function (401 no user, 403 non-admin); ammSchemas exports marketIdParam Joi schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /markets + GET /markets/:id (API-01), mounted in index.js</name>
  <files>VentureCast_Backend-main/controllers/ammController.js, VentureCast_Backend-main/routes/amm.js, VentureCast_Backend-main/index.js, VentureCast_Backend-main/tests/integration/amm.test.js</files>
  <behavior>
    - GET /markets with no JWT returns 200 and an array; each item has { marketId, streamerId,
      tier, status, supply, priceCents, reserveCents }.
    - priceCents equals priceCents(state.supply, {P0_cents,k_num,k_den}) for that market.
    - GET /markets/:id returns 200 with { market, marketState } for a real market; a syntactically
      invalid :id (not 24-hex) returns 400 (Joi marketIdParam); a well-formed-but-missing id returns 404.
  </behavior>
  <action>
    1. controllers/ammController.js — create with handlers `listMarkets(req,res)` and
       `getMarket(req,res)`, mirroring tradeController.js (try/catch; on a service error with
       `statusCode` return `res.status(err.statusCode).json({ error: err.message, ...err.details })`,
       else `logger.error(...)` + 500). For now inline the read logic (no separate service needed):
       - listMarkets: `const markets = await Market.find().lean();` then for each, fetch its
         MarketState (`MarketState.find({ marketId: { $in: ids } }).lean()`, build a map to avoid N+1),
         and map to `{ marketId: m._id, streamerId: m.streamerId, tier: m.tier, status: m.status,
         supply: st.supply, priceCents: priceCents(st.supply, {P0_cents:m.P0_cents,k_num:m.k_num,
         k_den:m.k_den}), reserveCents: st.reserveCents }`. Return `res.json(items)`.
       - getMarket: `const m = await Market.findById(req.params.id).lean()`; if !m return
         `404 { error: 'market not found' }`; fetch its MarketState; return
         `res.json({ market: m, marketState: st })`.
       Require Market, MarketState models, `{ priceCents }` from services/amm/pricing/curve, logger.
       Export { listMarkets, getMarket } (later plans add quote/order/portfolio/admin handlers here).
    2. routes/amm.js — create an express.Router mirroring trade.js. Require optionalAuth from
       middleware/auth, validate from middleware/validate, { marketIdParam } from
       middleware/schemas/ammSchemas, and ammController. Add:
         router.get('/markets', optionalAuth, ammController.listMarkets);
         router.get('/markets/:id', validate(marketIdParam, 'params'), optionalAuth, ammController.getMarket);
       (validate runs before optionalAuth so a malformed id 400s without a DB hit.) Export router.
    3. index.js — require the router (`const ammRoutes = require('./routes/amm');`) alongside the
       other route imports, and mount it in the "API Routes with rate limiters" block, BEFORE the
       404 handler, behind apiLimiter: `app.use('/', apiLimiter, ammRoutes);`. Do not reorder any
       existing middleware. (Quote/order POSTs in 05-02 will be guarded by tradeLimiter INSIDE the
       router via per-route limiter middleware, so apiLimiter at mount is fine.)
    4. amm.test.js — fill in the `GET /markets` describe block: (a) no-auth 200 + array of markets
       with the seven fields after createTestMarket(); (b) priceCents matches priceCents(supply,...);
       (c) GET /markets/:id 200 returns { market, marketState }; (d) GET /markets/<24-hex-missing> -> 404;
       (e) GET /markets/not-hex -> 400.
  </behavior>
  <verify>
    <automated>cd VentureCast_Backend-main && npx jest tests/integration/amm.test.js -t "GET /markets" --runInBand --forceExit</automated>
  </verify>
  <done>GET /markets (optionalAuth) lists markets with priceCents/supply/status/reserveCents; GET /markets/:id returns detail; malformed id 400, missing id 404; router mounted in index.js; tests green.</done>
</task>

</tasks>

<verification>
- `npx jest tests/integration/amm.test.js -t "GET /markets" --runInBand --forceExit` passes.
- `node -e "require('./index')"` boots without throwing (route mounted cleanly).
- requireAdmin + ammSchemas + User.isAdmin + generateAdminToken all importable.
</verification>

<success_criteria>
- API-01: GET /markets lists tradable markets with current price/supply/status (+ reserveCents).
- The foundation (router mounted, isAdmin field, requireAdmin, admin token helper, amm.test.js
  scaffold) exists so 05-02 and 05-03 extend it without re-touching index.js / User.js / fixtures.js.
</success_criteria>

<output>
After completion, create `.planning/phases/05-api-surface/05-01-SUMMARY.md`.
</output>
