---
phase: 06-simulation-full-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - VentureCast_Backend-main/tests/helpers/prng.js
  - VentureCast_Backend-main/tests/unit/simulation.test.js
  - VentureCast_Backend-main/tests/unit/execution.test.js
autonomous: true
requirements: [TEST-02, TEST-03, TEST-04]
must_haves:
  truths:
    - "A seeded PRNG produces a deterministic stream so any sim failure is reproducible from its logged seed"
    - "Invariant property tests assert after EVERY applied op: Σ cents ledger == 0, Σ shares ledger == 0, reserve >= floor, reserve >= 0"
    - "An unbalanced (tampered) posting set is rejected by assertBalanced"
    - "A 10,000-trade simulation runs for each tier ('1','2','3') driving the REAL pure engine functions"
    - "Across all three tiers the sim ends with a penny-balanced ledger and min observed reserve >= floor (never negative)"
    - "The concurrency invariant (5 concurrent executeOrder -> version+5, no lost update) is asserted (referenced/consolidated from execution.test.js)"
  artifacts:
    - path: "VentureCast_Backend-main/tests/helpers/prng.js"
      provides: "Deterministic seeded PRNG (mulberry32) — no Math.random, no external dep"
      min_lines: 15
      exports: ["mulberry32"]
    - path: "VentureCast_Backend-main/tests/unit/simulation.test.js"
      provides: "Invariant property tests (TEST-02) + 10k-per-tier sim (TEST-03)"
      min_lines: 150
      contains: "mulberry32"
  key_links:
    - from: "tests/unit/simulation.test.js"
      to: "services/amm/pricing/curve.js + fees.js + inversion.js"
      via: "require + direct call (buyCostCents/sellPayoutCents/applyBuyFees/applySellFees/cashToUnits)"
      pattern: "require.*amm/pricing"
    - from: "tests/unit/simulation.test.js"
      to: "services/amm/ledger/postings.js"
      via: "buildBuyPostings/buildSellPostings + assertBalanced applied to the SAME in-memory account deltas"
      pattern: "buildBuyPostings|buildSellPostings|assertBalanced"
    - from: "tests/unit/simulation.test.js"
      to: "services/amm/risk/index.js + config.riskConfigForTier"
      via: "risk.evaluate(trade, snapshot, riskConfigForTier(tier)) gates every iteration"
      pattern: "evaluate\\(|riskConfigForTier"
---

<objective>
Prove the AMM engine holds its invariants at scale. Build a deterministic, in-memory
simulation harness that drives the REAL pure engine functions (curve / fees / inversion /
ledger / risk) over 10,000 randomized buys/sells per tier and asserts the solvency
invariants after every applied trade. Add focused invariant property tests (including an
explicit "unbalanced posting is rejected" check), and consolidate the existing concurrency
assertion by reference.

Purpose: TEST-02 (invariant property tests), TEST-03 (10k-per-tier sim), TEST-04
(concurrency, by reference) are the capstone proof that the system never drains the reserve
or unbalances the ledger.

Output: tests/helpers/prng.js, tests/unit/simulation.test.js, and a small consolidating
reference assertion in tests/unit/execution.test.js. NO engine/API source edits.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-simulation-full-test-suite/06-VALIDATION.md

@VentureCast_Backend-main/services/amm/pricing/curve.js
@VentureCast_Backend-main/services/amm/pricing/fees.js
@VentureCast_Backend-main/services/amm/pricing/inversion.js
@VentureCast_Backend-main/services/amm/ledger/postings.js
@VentureCast_Backend-main/services/amm/risk/checks.js
@VentureCast_Backend-main/services/amm/config.js
@VentureCast_Backend-main/services/amm/execution/priceTrade.js
@VentureCast_Backend-main/tests/unit/execution.test.js

<interfaces>
<!-- Exact contracts the simulation must drive. Use these directly — no exploration. -->

services/amm/pricing/curve.js:
  buyCostCents(s0, delta, { P0_cents, k_num, k_den })   -> integer cents (ceil-rounded; s0 = supply BEFORE buy)
  sellPayoutCents(s0, delta, { P0_cents, k_num, k_den }) -> integer cents (floor-rounded; s0 = supply AFTER sell = supply - delta)
  priceCents(s, params)                                  -> integer cents (reference price; for circuit-breaker snapshot)

services/amm/pricing/fees.js:
  applyBuyFees(grossCents, { spreadBps, feeBps })  -> { grossCents, spreadCents, feeCents, totalCents }
  applySellFees(grossCents, { spreadBps, feeBps }) -> { grossCents, spreadCents, feeCents, netCents }   (throws if net<0)

services/amm/pricing/inversion.js:
  cashToUnits(X_cents, s0, params) -> { units, grossCents }   (for cash-denominated buys; mirror priceTrade)

services/amm/ledger/postings.js:
  buildBuyPostings({ userId, marketId, deltaQty, fees })  -> 5 postings (3 cents + 2 shares), unbalanced throws
  buildSellPostings({ userId, marketId, deltaQty, fees }) -> 5 postings
  assertBalanced(postings) -> postings | throws LedgerError(500, 'Ledger postings do not sum to zero')
  BUY  cents postings: user_cash -totalCents ; market_reserve +(grossCents+spreadCents) ; platform_fees +feeCents
  SELL cents postings: user_cash +netCents   ; market_reserve -(grossCents-spreadCents)  ; platform_fees +feeCents
  accountKey templates: user_cash:<uid> ; user_pos:<uid>:<mid> ; market_reserve:<mid> ;
                        market_shares_outstanding:<mid> ; platform_fees

services/amm/risk/index.js (re-exports checks.evaluate):
  evaluate(trade, snapshot, tierConfig) -> { allowed:true } | { allowed:false, riskError, riskEventDraft }
  trade:    { side:'buy'|'sell', grossCents, spreadCents, deltaQty, marketId, userId }
  snapshot: { reserveCents, reserveFloorCents, userPositionQty, userDailyVolumeCents,
              marketStatus:'active'|'paused', recentRefPriceCents, newPriceCents }
  tierConfig from config.riskConfigForTier(tier): { maxTradeCents, maxPositionQty, maxDailyCents, circuitBreakerPct }

services/amm/config.js:
  riskConfigForTier('1'|'2'|'3') -> { maxTradeCents, maxPositionQty, maxDailyCents, circuitBreakerPct }
  TIER_CONFIG['1'] caps: maxTradeCents 100000, maxPositionQty 1000, maxDailyCents 1000000, circuitBreakerPct 1000
  Defaults: DEFAULT_SPREAD_BPS=50, DEFAULT_FEE_BPS=100

services/amm/execution/priceTrade.js — MIRROR this composition exactly in the sim:
  BUY:  grossCents = buyCostCents(supply, delta) [or cashToUnits for cash]; fees=applyBuyFees;
        newReserve = reserve + (grossCents + spreadCents)
  SELL: grossCents = sellPayoutCents(supply - delta, delta); fees=applySellFees;
        newReserve = reserve - (grossCents - spreadCents)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seeded PRNG helper (mulberry32)</name>
  <files>VentureCast_Backend-main/tests/helpers/prng.js</files>
  <action>
    Create a deterministic seeded PRNG helper — CommonJS, no external dependency, no Math.random.
    Implement mulberry32(seed): returns a function rng() yielding a float in [0,1).
    Standard mulberry32 algorithm:
      function mulberry32(seed) {
        let a = seed >>> 0;
        return function () {
          a |= 0; a = (a + 0x6D2B79F5) | 0;
          let t = Math.imul(a ^ (a >>> 15), 1 | a);
          t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
          return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
        };
      }
    Also export two small helpers built on an rng:
      - randInt(rng, minInclusive, maxInclusive) -> integer in [min, max]
      - pick(rng, array) -> a random element
    module.exports = { mulberry32, randInt, pick }.
    Keep it pure and side-effect free.
  </action>
  <verify>
    <automated>cd VentureCast_Backend-main && node -e "const {mulberry32,randInt}=require('./tests/helpers/prng'); const r1=mulberry32(42), r2=mulberry32(42); const a=[r1(),r1(),r1()], b=[r2(),r2(),r2()]; if(JSON.stringify(a)!==JSON.stringify(b)) throw new Error('not deterministic'); if(!(a[0]>=0&&a[0]<1)) throw new Error('out of range'); console.log('prng ok', randInt(mulberry32(1),5,5));"</automated>
  </verify>
  <done>mulberry32(42) twice yields the identical stream; values in [0,1); randInt returns integers in range; no external import; no Math.random.</done>
</task>

<task type="auto">
  <name>Task 2: Invariant property tests + tampered-posting rejection (TEST-02)</name>
  <files>VentureCast_Backend-main/tests/unit/simulation.test.js</files>
  <action>
    Create tests/unit/simulation.test.js (PURE, in-memory — NO Mongoose, NO replset). Require the
    real engine modules and the prng helper from Task 1. Build a reusable in-memory "world" + a
    single applyTrade() step function that Task 3 also uses (define both at module top so both
    describe blocks share them).

    World shape (plain JS, integer-only):
      world = {
        marketId, tier, curveParams:{P0_cents,k_num,k_den}, feeParams:{spreadBps,feeBps},
        supply: 0, reserveCents, reserveFloorCents,
        accounts: Map<accountKey, number>,   // ledger projection (cents AND shares keys)
        positions: Map<userId, number>,       // convenience mirror of user_pos
        dailyVolume: Map<userId, number>,
      }
    Seed genesis into accounts the same way openMarket does conceptually: credit
    market_reserve:<mid> = reserveFloorCents and debit platform_funding = -reserveFloorCents so
    Σ cents == 0 at the start; set reserveCents = reserveFloorCents.

    applyTrade(world, { userId, side, deltaQty | cashCents }) MUST mirror priceTrade exactly:
      - BUY: grossCents = (cashCents != null) ? cashToUnits(cashCents, supply, curve).grossCents
             with deltaQty = its .units, else buyCostCents(supply, deltaQty, curve).
             fees = applyBuyFees(grossCents, feeParams). Build a Trade object
             { side, grossCents, spreadCents:fees.spreadCents, deltaQty, marketId, userId }.
      - SELL: only attempt if positions.get(userId) >= deltaQty (inventory guard, mirroring the
             orchestrator). grossCents = sellPayoutCents(supply - deltaQty, deltaQty, curve);
             fees = applySellFees(grossCents, feeParams).
      - Build snapshot { reserveCents, reserveFloorCents, userPositionQty: positions.get(userId)||0,
             userDailyVolumeCents: dailyVolume.get(userId)||0, marketStatus:'active',
             recentRefPriceCents: priceCents(supply, curve),
             newPriceCents: priceCents(side==='buy'? supply+deltaQty : supply-deltaQty, curve) }.
      - Call risk.evaluate(trade, snapshot, riskConfigForTier(world.tier)). If !allowed OR
        (sell with insufficient inventory) -> return { applied:false } (caller counts the reject).
      - Else build postings via buildBuyPostings/buildSellPostings (this also calls assertBalanced
        internally), then apply EACH posting delta to world.accounts (accounts.set(key, (get||0)+delta)).
        Update supply (+/- deltaQty), reserveCents (buy: +(gross+spread); sell: -(gross-spread)),
        positions, and dailyVolume (+= grossCents). Return { applied:true, trade, fees }.

    assertInvariants(world) helper:
      - Σ over all 'cents' accounts == 0   (sum every account whose key is a cents account:
        user_cash:*, market_reserve:*, platform_fees, platform_funding)
      - Σ over all 'shares' accounts == 0  (user_pos:*, market_shares_outstanding:*)
        NOTE: simplest is to track unit per accountKey via a known-prefix classifier helper.
      - world.reserveCents >= world.reserveFloorCents
      - world.reserveCents >= 0
      - world.accounts.get('market_reserve:'+world.marketId) === world.reserveCents

    describe('invariant property tests (TEST-02)'):
      - it('holds all invariants after every op over a randomized run'): seed mulberry32(12345),
        build a tier-1 world, run ~500 randomized ops (random user from a small pool, random side,
        random small size within tier-1 caps; bias toward buys early so sells have inventory),
        call assertInvariants(world) after EVERY applied trade. Log the seed in the test name/console
        so a failure reproduces.
      - it('assertBalanced rejects a tampered/unbalanced posting set'): build a valid buy postings
        array, mutate one cents delta by +1, expect(() => assertBalanced(tampered)).toThrow(); assert
        the thrown error has statusCode 500 / message 'Ledger postings do not sum to zero'.

    Export applyTrade / makeWorld / assertInvariants via module-scope so Task 3's describe reuses them
    (do NOT module.exports from a .test.js — just define at top-level of the file).
  </action>
  <verify>
    <automated>cd VentureCast_Backend-main && npx jest tests/unit/simulation.test.js -t "invariant" --runInBand --forceExit</automated>
  </verify>
  <done>The "invariant property tests (TEST-02)" describe passes: invariants hold after every applied op across a seeded randomized run, and a tampered posting set throws via assertBalanced (statusCode 500).</done>
</task>

<task type="auto">
  <name>Task 3: 10k-trades-per-tier simulation (TEST-03) + concurrency reference (TEST-04)</name>
  <files>VentureCast_Backend-main/tests/unit/simulation.test.js, VentureCast_Backend-main/tests/unit/execution.test.js</files>
  <action>
    In tests/unit/simulation.test.js add describe('10000-trades-per-tier simulation (TEST-03)') that
    reuses makeWorld/applyTrade/assertInvariants from Task 2.

    For EACH tier of ['1','2','3'] add an it(`tier ${tier}: 10000 trades stay solvent and balanced`):
      - SEED = a fixed per-tier seed (e.g. 1000 + Number(tier)); log it. rng = mulberry32(SEED).
      - Build a tier world (P0_cents 100, k_num 1, k_den 10, spread 50 / fee 100, reserveFloorCents
        e.g. 10000). Use a pool of ~10 users.
      - Loop 10,000 iterations: pick a random user; random side. For BUYS pick a small qty whose
        gross stays UNDER the tier circuit-breaker AND under maxTradeCents (keep qty small, e.g.
        randInt 1..50, so a single trade never trips the price-move breaker at this curve). For SELLS
        choose qty <= positions.get(user) (skip/treat as reject if user owns 0). Call applyTrade.
      - Track fillCount / rejectCount and minReserve = Math.min(minReserve, world.reserveCents) after
        each APPLIED trade. Call assertInvariants(world) after EVERY applied trade (the per-op proof).
      - After the loop assert: assertInvariants(world) once more; minReserve >= world.reserveFloorCents;
        world.reserveCents >= 0; Σ cents == 0 and Σ shares == 0; fillCount > 0 (the run actually did
        work). Console.log a one-line report: `tier ${tier} seed=${SEED} fills=${fillCount} rejects=${rejectCount} minReserve=${minReserve}`.
      - Set a generous jest timeout (e.g. 30000ms) for these heavier its.

    Keep total runtime well under a few seconds (30k pure iterations, no I/O).

    In tests/unit/execution.test.js (TEST-04 consolidation): the existing
    'version / concurrency (EXEC-02)' describe already proves "N concurrent buys -> version += N, no
    lost update". Do NOT duplicate it. Add ONE consolidating assertion inside that existing
    'N concurrent buys...' it (or a tightly-scoped sibling it in the same describe) that explicitly
    asserts the no-lost-update conservation for TEST-04: after the N concurrent buys, assert
    Σ shares ledger == 0 AND the user_pos projection equals after.supply AND Trade count === N — a
    single consolidating "no lost update" assertion referencing the concurrency scenario. Add a brief
    comment tagging it `// TEST-04 (concurrency / no lost update) consolidation`.
  </action>
  <verify>
    <automated>cd VentureCast_Backend-main && npx jest tests/unit/simulation.test.js --runInBand --forceExit && npx jest tests/unit/execution.test.js -t "concurren" --runInBand --forceExit</automated>
  </verify>
  <done>The 10k-per-tier sim passes for tiers 1/2/3 with min reserve >= floor (never negative), penny-balanced ledger (Σ cents==0, Σ shares==0), and fillCount>0; per-tier seed/fill/reject/minReserve report logged. The existing concurrency test still passes with the added TEST-04 no-lost-update consolidating assertion.</done>
</task>

</tasks>

<verification>
- `cd VentureCast_Backend-main && npx jest tests/unit/simulation.test.js --runInBand --forceExit` is green (invariants + 10k×3 tiers).
- `cd VentureCast_Backend-main && npx jest tests/unit/execution.test.js -t "concurren" --runInBand --forceExit` is green (TEST-04 reference + consolidation).
- The sim imports and CALLS the real curve/fees/inversion/ledger/risk functions — it is NOT a reimplementation (grep shows require of services/amm/*).
- Every applied trade is followed by an invariant assertion; the run is seeded (logged) and deterministic.
- No edits under services/ or routes/ or controllers/ (only tests/).
</verification>

<success_criteria>
- TEST-02: invariant property tests assert Σ cents==0, Σ shares==0, reserve>=floor, reserve>=0 after every op, plus tampered-posting rejection.
- TEST-03: 10,000 trades per tier (1/2/3) finish with no negative reserve, min reserve>=floor, and a penny-balanced ledger.
- TEST-04: concurrency no-lost-update is asserted by reference + one consolidating assertion (no duplication of the existing test).
- Deterministic: a fixed seed reproduces any failure; the seed is logged.
- Scope honored: test files + prng helper only; zero engine/API source changes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-simulation-full-test-suite/06-01-SUMMARY.md`.
If the simulation surfaces a REAL invariant breach in the engine, do NOT silently edit source —
record it as a FINDING in the summary (seed + tier + first failing iteration) for follow-up.
</output>
