---
phase: 06-simulation-full-test-suite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - VentureCast_Backend-main/tests/integration/flow.test.js
autonomous: true
requirements: [TEST-05]
must_haves:
  truths:
    - "A real JWT user can POST /quotes and receive a priced, expiring quote (buy + sell shapes)"
    - "A POST /orders buy executes atomically and returns { trade, balances } with positionQty > 0"
    - "GET /portfolio reflects the position created by the buy, IDOR-safe (JWT user only)"
    - "A POST /orders sell reduces the position and GET /portfolio shows the reduced qty"
    - "Replaying POST /orders with the same idempotencyKey returns the identical trade (replayed:true), one Trade total"
    - "Quote-expiry rejection (409) and slippage rejection (409) are asserted at the HTTP layer (referencing amm.test.js coverage)"
  artifacts:
    - path: "VentureCast_Backend-main/tests/integration/flow.test.js"
      provides: "End-to-end supertest walk quote -> order(buy) -> portfolio -> order(sell) -> portfolio + replay (TEST-05)"
      min_lines: 100
      contains: "quote"
  key_links:
    - from: "tests/integration/flow.test.js"
      to: "POST /quotes, POST /orders, GET /portfolio (index.js app)"
      via: "supertest request(app) with Bearer JWT from generateAuthToken"
      pattern: "request\\(app\\)"
    - from: "tests/integration/flow.test.js"
      to: "MongoMemoryReplSet via helpers/db.js"
      via: "connectTestDB + Model.init() before transactions, clearTestDB between"
      pattern: "connectTestDB|Model.init|\\.init\\(\\)"
---

<objective>
Prove the contract the frontend will consume: a single supertest walk over the real HTTP
stack — POST /quotes -> POST /orders (buy) -> GET /portfolio (position present) ->
POST /orders (sell) -> GET /portfolio (reduced) — plus a same-idempotencyKey replay that
returns the identical trade, and HTTP-level quote-expiry + slippage rejection.

Purpose: TEST-05 — idempotency-replay, quote-expiry, and slippage-rejection all pass, and the
end-to-end flow is green (ROADMAP success criterion 5).

Output: tests/integration/flow.test.js. NO engine/API source edits — Phase 6 only validates.
</objective>

<execution_context>
@/Users/anishguntreddi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anishguntreddi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-simulation-full-test-suite/06-VALIDATION.md

@VentureCast_Backend-main/tests/integration/amm.test.js
@VentureCast_Backend-main/tests/helpers/db.js
@VentureCast_Backend-main/tests/helpers/fixtures.js
@VentureCast_Backend-main/tests/helpers/ammFixtures.js

<interfaces>
<!-- Exact HTTP contracts (extracted from amm.test.js). Use these directly. -->

Test harness (mirror amm.test.js exactly):
  const request = require('supertest');
  const app = require('../../index');                       // require.main guard → no listen
  const { connectTestDB, clearTestDB, disconnectTestDB } = require('../helpers/db');
  const { createTestUser, generateAuthToken } = require('../helpers/fixtures');
  const { createTestMarket } = require('../helpers/ammFixtures');
  beforeAll -> connectTestDB(); afterEach -> clearTestDB(); afterAll -> disconnectTestDB();
  IMPORTANT (project gotcha): before any POST /orders txn, await Promise.all([... Model.init()])
  for Market, MarketState, Trade, Order, LedgerAccount (see amm.test.js POST /orders beforeEach).

  createTestMarket({ P0_cents:100, k_num:1, k_den:10 }) -> { market, marketState }
  createTestUser() -> User doc ; generateAuthToken(user._id) -> Bearer token

POST /quotes  (auth) body { marketId, side:'buy'|'sell', qty | cashCents }
  buy 200 -> { quoteId, marketId, side, qty, avgPriceCents, grossCents, feeCents, spreadCents,
               totalCents, priceImpactBps, expiresAt }   (sell -> netCents instead of totalCents)

POST /orders  (auth) body { marketId, side, qty | cashCents, idempotencyKey, maxCostCents?, minReceivedCents? }
  200 buy -> { trade:{_id, side, ...}, balances:{ cashCents, positionQty }, replayed:false }
  replay same idempotencyKey -> { trade:{ _id == original }, replayed:true }  (one Trade total)
  buy maxCostCents too low -> 409 ; sell minReceivedCents too high -> 409
  NOTE: cash-denominated buys (cashCents) keep trade size small → stays under tier-1 circuit breaker.

GET /portfolio (auth, IDOR-safe, no :userId) -> 200 { cashCents, positions:[{ marketId,
  streamerId, positionQty, priceCents, valueCents }], totalValueCents }

Quote-expiry at execution time is enforced via a past quoteExpiresAt (orchestrator rejects 409);
amm.test.js exercises slippage 409s — reference that coverage, the flow walk adds the replay + a
quote→order happy path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end flow walk (quote -> buy -> portfolio -> sell -> portfolio) + replay</name>
  <files>VentureCast_Backend-main/tests/integration/flow.test.js</files>
  <action>
    Create tests/integration/flow.test.js using the EXACT harness shape from amm.test.js
    (supertest + require('../../index') app + MongoMemoryReplSet via helpers/db). In beforeAll
    connectTestDB(); afterEach clearTestDB(); afterAll disconnectTestDB(). In a beforeEach (for the
    order-executing describe) await Promise.all([Market.init(), MarketState.init(), Trade.init(),
    Order.init(), LedgerAccount.init()]) — required for Mongoose transactions (project gotcha).

    describe('end-to-end AMM flow (TEST-05)'):
      Setup per test: const user = await createTestUser(); const token = generateAuthToken(user._id);
      const { market } = await createTestMarket({ P0_cents:100, k_num:1, k_den:10 }); const mid = market._id.toString();
      Use Bearer ${token} on every request. Keep buys SMALL (cashCents, e.g. 500) so trade size
      stays under the tier-1 circuit breaker / maxTradeCents.

      it('walks quote -> buy -> portfolio -> sell -> portfolio'):
        1. POST /quotes { marketId:mid, side:'buy', cashCents:500 } -> 200; assert quoteId,
           avgPriceCents, totalCents present; expiresAt is in the future.
        2. POST /orders { marketId:mid, side:'buy', cashCents:500, idempotencyKey:<unique> } -> 200;
           assert body.trade._id, body.trade.side==='buy', body.balances.positionQty > 0, replayed:false.
           Capture boughtQty = body.balances.positionQty and tradeId = body.trade._id.
        3. GET /portfolio (token) -> 200; find positions[ p.marketId === mid ]; assert it exists and
           positionQty === boughtQty, priceCents>0, valueCents>0.
        4. POST /quotes { marketId:mid, side:'sell', qty: <half of boughtQty, min 1> } -> 200; assert
           netCents present and NOT totalCents (sell shape).
        5. POST /orders { marketId:mid, side:'sell', qty:<same half>, idempotencyKey:<unique> } -> 200;
           assert trade.side==='sell'.
        6. GET /portfolio -> 200; assert the position's positionQty === boughtQty - soldQty (reduced),
           and is < boughtQty.

      it('same-idempotencyKey replay returns the identical trade (TEST-05)'):
        const ik = `flow-replay-${Date.now()}`; body = { marketId:mid, side:'buy', cashCents:500, idempotencyKey:ik }.
        POST /orders body -> 200 (replayed:false). POST /orders SAME body -> 200; assert
        res2.body.trade._id === res1.body.trade._id AND res2.body.replayed === true. Then assert
        await Trade.countDocuments({ marketId: market._id }) === 1 (never re-executed).

    Use unique idempotencyKeys per order (Date.now()/counter) except the deliberate replay pair.
  </action>
  <verify>
    <automated>cd VentureCast_Backend-main && npx jest tests/integration/flow.test.js -t "walks|replay" --runInBand --forceExit</automated>
  </verify>
  <done>The flow walk is green: quote (buy+sell shapes), buy creates a position visible in /portfolio, sell reduces it, and the same-idempotencyKey replay returns the identical trade with replayed:true and exactly one Trade document.</done>
</task>

<task type="auto">
  <name>Task 2: HTTP-level quote-expiry + slippage rejection (TEST-05)</name>
  <files>VentureCast_Backend-main/tests/integration/flow.test.js</files>
  <action>
    In the SAME tests/integration/flow.test.js add describe('expiry + slippage rejection (TEST-05)')
    that asserts the rejection contract at the HTTP layer (referencing the coverage already present
    in amm.test.js — this consolidates expiry+slippage into the flow file so TEST-05 has a single
    cohesive home; do not duplicate every amm.test.js case).

      it('rejects a buy whose quote has expired (409)'):
        POST /orders { marketId:mid, side:'buy', cashCents:500, idempotencyKey:<unique>,
        quoteExpiresAt: new Date(Date.now() - 60000).toISOString() } -> expect 409. Assert no Trade
        was created for that key (Trade.countDocuments unchanged / 0 for this market path as appropriate).

      it('rejects a buy with maxCostCents below the real cost (409 slippage)'):
        POST /orders { marketId:mid, side:'buy', cashCents:500, idempotencyKey:<unique>,
        maxCostCents: 1 } -> expect 409.

      it('rejects a sell with minReceivedCents above the real payout (409 slippage)'):
        First give the user a position: POST /orders buy { cashCents:500, idempotencyKey:<unique> } -> 200,
        read positionQty. Then POST /orders { side:'sell', qty:1, idempotencyKey:<unique>,
        minReceivedCents: 999_999_999 } -> expect 409.

    Confirm whether the orchestrator accepts a `quoteExpiresAt` field on the POST /orders body (it is
    the same param executeOrder consumes in execution.test.js). If the /orders Joi schema strips it,
    fall back to asserting expiry at the executeOrder layer is already covered by execution.test.js and
    instead assert the two slippage 409s here (keep the expiry it but mark it referencing
    execution.test.js coverage if the HTTP route does not surface quoteExpiresAt). Prefer the HTTP
    assertion when the route accepts it.
  </action>
  <verify>
    <automated>cd VentureCast_Backend-main && npx jest tests/integration/flow.test.js -t "expiry|slippage|rejection" --runInBand --forceExit</automated>
  </verify>
  <done>Slippage rejections (buy maxCostCents too low, sell minReceivedCents too high) return 409 at the HTTP layer; quote-expiry rejection is asserted (HTTP 409 if the route surfaces quoteExpiresAt, otherwise documented as covered by execution.test.js). No source edits.</done>
</task>

</tasks>

<verification>
- `cd VentureCast_Backend-main && npx jest tests/integration/flow.test.js --runInBand --forceExit` is green.
- The walk exercises the REAL HTTP stack (require('../../index') app, real JWT, MongoMemoryReplSet) end-to-end.
- Replay returns the identical trade and exactly one Trade document persists.
- Quote-expiry + slippage rejections are asserted (TEST-05).
- Only tests/integration/flow.test.js is added — no edits under services/, routes/, controllers/, models/.
</verification>

<success_criteria>
- TEST-05: idempotency-replay (same key -> one trade, identical response), quote-expiry rejection, and slippage rejection all pass.
- ROADMAP criterion 5: the supertest walk quote -> order -> portfolio -> sell is green end-to-end.
- Harness mirrors amm.test.js (Model.init() before txns, clearTestDB between) — no flaky txn setup.
- Scope honored: single new test file, zero source changes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-simulation-full-test-suite/06-02-SUMMARY.md`.
If the flow surfaces a REAL contract bug, record it as a FINDING (not a silent source edit).
</output>
